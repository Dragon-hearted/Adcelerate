import { Config } from "@remotion/cli/config";
// No special config needed; defaults are good for GIF rendering.
// Uncomment below to override the public dir or output location if needed:
// Config.setPublicDir("./public");
// Config.setOutputLocation("out/");
