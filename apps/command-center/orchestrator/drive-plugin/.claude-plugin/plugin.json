{
  "name": "drive-plugin",
  "description": "Drive-mode skill loader (slice #39). Exposes adcelerate-execute to orchestrator Drive sessions as a repo-local plugin so settingSources:[] stays — no project settings/permissions/hooks loaded, canUseTool gate uncircumventable (ADR-0002).",
  "version": "0.0.0"
}
