# Orchestrator base URL (Fastify + Socket.IO). Used by Next rewrites (server) and
# the browser Socket.IO client. Defaults to the local orchestrator.
ORCH_URL=http://127.0.0.1:4100
NEXT_PUBLIC_ORCH_URL=http://127.0.0.1:4100
